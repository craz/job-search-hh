#!/usr/bin/env bash
# One explicit `direnv allow` enables repository-local Python automatically.
source_env_if_exists .env.local
./scripts/ensure-venv.sh
export VIRTUAL_ENV="$PWD/.venv"
PATH_add .venv/bin

